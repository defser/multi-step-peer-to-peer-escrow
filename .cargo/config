[alias]
wasm = "build --release --lib --target wasm32-unknown-unknown"
wasm-debug = "build --lib --target wasm32-unknown-unknown"
unit-test = "test --lib --test unit"
integration-test = "test --lib --test integration"
schema = "run --bin schema"
